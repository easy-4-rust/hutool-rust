# hutool-bloom-filter 与 hutool-bloom-filter 对象名称一致性检查

> 检查时间：2026-07-27
> 基线：Hutool 5.8.x `cn.hutool.bloomFilter.*`（19 个 Java 类，3 个子包）
> hutool-bloom-filter crate：5 个 `.rs` 文件

## 统计汇总

| 维度 | Hutool | hutool-bloom-filter | 说明 |
|------|--------|---------------------|------|
| Java 类总数 | 19 | — | 排除 `package-info` |
| Rust 文件总数 | — | 5 | 排除 `mod.rs` / `lib.rs` |
| **完全匹配** | — | **0** ⚠️ | 19 个全部聚合 |
| Hutool 有 hutool-bloom-filter 没有 | 19 | — | S6 拆分后全部补齐 |
| hutool-bloom-filter 有 Hutool 没有 | — | 3 | Rust 特有（🆕） |
| 已合并（🔀） | — | 19 | 全部在 `bitmap.rs` / `hashes.rs` / `hutool.rs` |

> **关键问题**：当前 0 个完全匹配，因为所有 19 个 Java 类都聚合在 3 个 .rs 文件中。
> 这是 hutool-rust "9 个 compat.rs 单源 dump 违规 crate 之一"。S6 拆分完成后将变为
> 19 个完全匹配。

---

## 一、当前文件归属（拆分前）

### 1.1 `bitmap.rs`（143 行，3 个类）

| Java 类 | 状态 |
|---------|------|
| `BitMap`（接口） | 🔀 |
| `IntMap` | 🔀 |
| `LongMap` | 🔀 |

### 1.2 `hashes.rs`（188 行，10 个类）

| Java 类 | 状态 |
|---------|------|
| `DefaultFilter` | 🔀 |
| `ELFFilter` | 🔀 |
| `FNVFilter` | 🔀 |
| `HfFilter` | 🔀 |
| `HfIpFilter` | 🔀 |
| `JSFilter` | 🔀 |
| `PJWFilter` | 🔀 |
| `RSFilter` | 🔀 |
| `SDBMFilter` | 🔀 |
| `TianlFilter` | 🔀 |

### 1.3 `hutool.rs`（384 行，6 个类）

| Java 类 | 状态 |
|---------|------|
| `BloomFilter`（接口） | 🔀 |
| `BitMapBloomFilter` | 🔀 |
| `BitSetBloomFilter` | 🔀 |
| `BloomFilterUtil` | 🔀 |
| `FuncFilter` | 🔀 |
| `AbstractFilter` | 🔀 |

### 1.4 `lib.rs`（128 行，0 个类）

`lib.rs` 仅做 `mod` 声明 + `pub use` re-export，符合 AGENTS.md 要求。

### 1.5 `tests.rs`（242 行，0 个类）

集成测试聚合在 `tests.rs` 单文件，每 Filter 至少 1 个 happy-path 单测。

---

## 二、S6 拆分后目标（19 个完全匹配）

### 2.1 位图子包（3 个 ✅）

| Hutool | hutool-bloom-filter | 状态 |
|--------|---------------------|------|
| `BitMap` | `bitmap.rs` | ✅ |
| `IntMap` | `int_map.rs` | ✅ |
| `LongMap` | `long_map.rs` | ✅ |

### 2.2 哈希过滤器子包（12 个 ✅）

| Hutool | hutool-bloom-filter | 状态 |
|--------|---------------------|------|
| `AbstractFilter` | `abstract_filter.rs` | ✅ |
| `DefaultFilter` | `default_filter.rs` | ✅ |
| `ELFFilter` | `elf_filter.rs` | ✅ |
| `FNVFilter` | `fnv_filter.rs` | ✅ |
| `FuncFilter` | `func_filter.rs` | ✅ |
| `HfFilter` | `hf_filter.rs` | ✅ |
| `HfIpFilter` | `hf_ip_filter.rs` | ✅ |
| `JSFilter` | `js_filter.rs` | ✅ |
| `PJWFilter` | `pjw_filter.rs` | ✅ |
| `RSFilter` | `rs_filter.rs` | ✅ |
| `SDBMFilter` | `sdbm_filter.rs` | ✅ |
| `TianlFilter` | `tianl_filter.rs` | ✅ |

### 2.3 根包子包（4 个 ✅）

| Hutool | hutool-bloom-filter | 状态 |
|--------|---------------------|------|
| `BloomFilter` | `string_bloom_filter.rs` | ✅ |
| `BitMapBloomFilter` | `bit_map_bloom_filter.rs` | ✅ |
| `BitSetBloomFilter` | `bit_set_bloom_filter.rs` | ✅ |
| `BloomFilterUtil` | `bloom_filter_util.rs` | ✅ |

---

## 三、Rust 特有（🆕 3 个，无 Java 对偶）

| Rust 文件 | 语义 | 替代方案 |
|-----------|------|----------|
| `machine_word.rs` | `MachineWord::Bits32/Bits64` 位宽枚举 | Java 用 `int[]` / `long[]` 区分 |
| `bloom_filter.rs` | `BloomFilter<T>` 泛型生产级布隆过滤器 | Java `BloomFilter` 仅支持 String |
| `bloom_filter_error.rs` | `BloomFilterError` 错误枚举 | Java 用 `IllegalArgumentException` |

---

## 四、命名风格检查

| 检查项 | Java | Rust | 结论 |
|--------|------|------|------|
| 类型命名 | `PascalCase` | `PascalCase` | ✅ 一致 |
| 方法命名 | `camelCase` | `snake_case` | ✅ 符合 Rust 惯例 |
| 字段命名 | `camelCase` | `snake_case` | ✅ 符合 Rust 惯例 |
| 常量命名 | `UPPER_SNAKE_CASE` | `UPPER_SNAKE_CASE` | ✅ 一致 |
| 文件名 | `PascalCase.java` | `snake_case.rs` | ✅ 符合 Rust 惯例 |
| 目录名 | 小写单词 | `snake_case` | ✅ 一致 |
| 抽象前缀 | `Abstract*` | `Abstract*` | ✅ 一致（`AbstractFilter`） |
| 异常后缀 | `*Exception` | `*Exception` | ⚠️ Rust 改用 `*Error` |
| 工具后缀 | `*Util` | `*Util` | ✅ 一致（`BloomFilterUtil`） |

> 注：Java 的 `Exception` 后缀在 Rust 端按惯例改为 `Error`，见 `BloomFilterError`。
> 这是 Rust 标准命名风格。

---

## 五、待办（路线图后续阶段）

| 阶段 | 待补 | 优先级 |
|------|------|-------|
| S6 | 拆分 `hutool.rs` 为 5 个文件 | 🔴 高（违反红线） |
| S6 | 拆分 `hashes.rs` 为 10 个文件 | 🔴 高 |
| S6 | 拆分 `bitmap.rs` 为 3 个文件 | 🟡 中 |
| S7 | 每个新文件中文头注释 | 🟡 中 |

---

## 六、与 AGENTS.md 红线对照

| 红线 | 当前状态 | 违规 |
|------|---------|------|
| ❌ 单文件不得超过 200 行 | `hutool.rs` 384 行 / `hashes.rs` 188 行 / `bitmap.rs` 143 行 | ✅ 违规 |
| ❌ 不得用单一 `compat.rs` 集中定义所有对象 | `hutool.rs` 集中 6 个类 | ✅ 违规 |
| ✅ 不允许 `unimplemented!()` / `todo!()` | 已确认无 | ✅ 合规 |
| ✅ 每个 .rs 文件对应一个 Java 对象 | 当前 5:19，**不满足** | ❌ 违规 |

> S6 拆分完成后，以上红线全部 ✅ 合规。

---

## 七、模块结构（拆分后目标）

```
hutool-bloom-filter/
├── Cargo.toml
└── src/
    ├── lib.rs                    # crate 门面（mod 声明 + re-export）
    ├── bitmap.rs                 # BitMap trait
    ├── int_map.rs                # IntMap struct
    ├── long_map.rs               # LongMap struct
    ├── machine_word.rs           # MachineWord enum
    ├── bloom_filter.rs           # BloomFilter<T> 泛型生产级
    ├── bloom_filter_error.rs     # BloomFilterError
    ├── string_bloom_filter.rs    # StringBloomFilter trait
    ├── func_filter.rs            # FuncFilter
    ├── abstract_filter.rs        # AbstractFilter
    ├── bit_map_bloom_filter.rs   # BitMapBloomFilter
    ├── bit_set_bloom_filter.rs   # BitSetBloomFilter
    ├── bloom_filter_util.rs      # BloomFilterUtil
    ├── default_filter.rs         # 12 个具名 Filter
    ├── elf_filter.rs
    ├── fnv_filter.rs
    ├── hf_filter.rs
    ├── hf_ip_filter.rs
    ├── js_filter.rs
    ├── pjw_filter.rs
    ├── rs_filter.rs
    ├── sdbm_filter.rs
    ├── tianl_filter.rs
    ├── hashes/
    │   └── mod.rs                # 12 个哈希常量（仅 mod 声明）
    └── tests/
        └── mod.rs                # 集成测试（按类拆分）
```

---

## 八、状态汇总

| 维度 | 当前 | S6 拆分后目标 |
|------|------|--------------|
| Rust 文件数 | 5 | 22+ |
| 完全匹配 | 0 | 19 ✅ |
| 🔀 合并 | 19 | 0 |
| 🆕 Rust 特有 | 3 | 3 |
| 单文件最大行数 | 384 | ≤ 100 |
| AGENTS.md 合规 | ❌ | ✅ |

---

> 文档维护：每次新增/拆分/合并 .rs 文件时同步更新本检查表
