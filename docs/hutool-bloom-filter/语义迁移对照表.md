# hutool-bloomFilter Java → Rust 功能语义迁移对照表

> 生成日期：2026-07-29
> Java 基线：`a0bd223dc0d036f55cfe4d8e2f5737ddc31f2b12`
> Rust 基线：`9f3215bf4c4df716831b18f32a57bca8eea68409`
> 最近按 Rust 基线审计：2026-07-29
> 文档状态：`DRAFT_AUDITED_STATIC`
> Java 模块：`hutool-bloomFilter`
> Rust 目标：`crates/hutool-bloom-filter`
> 兼容目标：公共 API、功能语义与可观察行为完整迁移；Rust 机制允许不同，但不得删减合同。
> CodeGraph：Java `files=2306 nodes=44769 edges=100575 db=117.46MB status=up-to-date`；Rust `files=2053 nodes=28492 edges=74236 db=80.38MB status=up-to-date`。
> 证据边界：CodeGraph 提供 E2 静态符号/关系证据；正则清单用于完整主对象盘点。二者都不是运行时差分证明，不能据此标记 `BEHAVIOR_VERIFIED`。

本文回答“行为合同是否存在、Rust 如何承载、证据到哪一级”。本轮没有把单端 Rust 测试或文件同名当作差分证明。

## 一、能力族与调用边界
| Java 包能力族 | 对象数 | Rust 候选状态 | 关键语义合同 | 下一证据 |
|---|---:|---|---|---|
| `filter` | 12 | IU=1 / SK=0 / NS=11 | 输入/默认值/错误/顺序/副作用/资源与线程安全 | 对象级调用图 + 源测试处置 + E3/E4 |
| `bloomfilter` | 4 | IU=4 / SK=0 / NS=0 | 输入/默认值/错误/顺序/副作用/资源与线程安全 | 对象级调用图 + 源测试处置 + E3/E4 |
| `bitMap` | 3 | IU=1 / SK=0 / NS=2 | 输入/默认值/错误/顺序/副作用/资源与线程安全 | 对象级调用图 + 源测试处置 + E3/E4 |

## 二、类型、所有权与可空性
| Java 语义 | Rust 映射原则 | 当前证据状态 | 必须验证 |
|---|---|---|---|
| nullable reference / `null` | `Option<T>`，不得用空串/零值偷偷代替 | `IMPLEMENTED_UNVERIFIED` | null、缺省、显式空三态 |
| mutable bean | struct + 受控 setter/builder | `IMPLEMENTED_UNVERIFIED` | Java setter 副作用与校验顺序 |
| interface/abstract class | trait + 组合状态/默认方法 | `IMPLEMENTED_UNVERIFIED` | object safety、默认实现、动态选择 |
| Java 泛型/通配符 | Rust 泛型、trait bound、`dyn Trait` | `IMPLEMENTED_UNVERIFIED` | 方差、擦除、运行时类型失败 |

## 三、方法、重载与参数
| 指标 | 当前值 | 结论 |
|---|---:|---|
| Java 公开/保护签名（静态解析） | 53 | 每个签名已在对象表列出，需语言级复核 |
| 发生重载的方法族 | 16 | 不允许无损证明前合并为单一 convenience API |
| Rust 公共函数候选 | 32 | 名称命中不等于默认值/错误/副作用一致 |

重载迁移规则：一个基础语义保留 canonical snake_case 名；差异使用 `_with_*`、`_from_*`、options、trait 或 builder；Java 参数名按 snake_case 保持顺序，可空性显式使用 `Option`。

## 四、错误体系
| Java 异常对象 | Rust 候选 | 状态 | 必须验证 |
|---|---|---|---|
| 模块无独立异常对象或由上层声明 | typed `Result<T, E>` | `IMPLEMENTED_UNVERIFIED` | 所有失败分支不得 panic 化 |

## 五、序列化与数据格式
| 检测 | 当前 Rust 形态 | 状态 | 验收 |
|---|---|---|---|
| Java 序列化/XML/JSON 痕迹：有 | `serde`/格式 crate 是否直接声明：否或经内部 crate 间接提供` | `IMPLEMENTED_UNVERIFIED` | 字段名、alias、null/default、顺序、数字、未知字段和 Java golden wire |

## 六、集合、顺序与数值
| Java 合同 | Rust 选择 | 状态 | 边界测试 |
|---|---|---|---|
| List/数组 | `Vec`/slice/iterator | `IMPLEMENTED_UNVERIFIED` | 空、重复、稳定顺序、越界 |
| Map/Set | Hash/Tree/Index 系列按合同选择 | `IMPLEMENTED_UNVERIFIED` | 迭代顺序、键相等、重复覆盖 |
| number/BigDecimal/time | checked 数值、`rust_decimal`/chrono 等按模块需要 | `IMPLEMENTED_UNVERIFIED` | 溢出、舍入、NaN、时区、locale |

## 七、并发、异步与生命周期
| Java 检测 | Rust 机制 | 状态 | 必须验证 |
|---|---|---|---|
| 并发/线程痕迹：未检出 | 所有权优先，必要时 `Arc`/锁/atomic/Tokio/channel | `IMPLEMENTED_UNVERIFIED` | 原子复合操作、取消、背压、超时、关闭、资源泄漏 |
| I/O/宿主痕迹：有 | RAII + typed error + 显式 async/blocking 边界 | `IMPLEMENTED_UNVERIFIED` | 失败注入、部分写入、重试归属、Drop/close |

## 八、SPI、反射与动态分派
| Java 检测 | Rust 替代候选 | 状态 | 门禁 |
|---|---|---|---|
| ServiceLoader/反射/代理痕迹：未检出 | 显式 registry/factory/trait；仅确需链接期注册时用 `inventory` | `IMPLEMENTED_UNVERIFIED` | 选择顺序、失败跳过、重复注册、生命周期、可扩展性 |

## 九、注解与宏
| Java 检测 | Rust 边界 | 状态 | 验收 |
|---|---|---|---|
| 注解/元数据痕迹：有 | runtime trait/registry 与 `hutool-macro` proc-macro 分离 | `IMPLEMENTED_UNVERIFIED` | expansion success/fail、generics、visibility、renamed dependency、adapter re-export |

## 十、组件替换与依赖状态
| 来源 | 组件 | 当前状态 | 尚缺证据 |
|---|---|---|---|
| Java POM | `hutool-core` | `SOURCE_DECLARED` | 精确调用合同、失败/生命周期/安全 |
| Rust Cargo | `bloomfilter, encoding_rs, tempfile, thiserror` | `DEPENDENCY_DECLARED` | license、MSRV 1.94、targets、advisory、unsafe/build.rs、POC、host |
| 采用原则 | std 优先，其次聚焦 crate，框架保持在宿主层 | `POLICY_ONLY` | 当前证据不允许晋级 `CONTRACT_VERIFIED` |

## 十一、宿主、示例与文档
| 资产 | 当前状态 | 下一步 |
|---|---|---|
| Java 测试文件 3 个 | 未建立逐用例 SOURCE_PARITY 处置 | 记录 mirrored/golden/live/不适用/受阻 |
| Rust 集成测试文件 1 个 | E1/E2 候选 | 绑定对象和 Java 测试来源，不伪称差分 |
| 业务宿主/真实依赖 | 未在本轮执行 | 选择真实网络、DB、文件、格式或框架宿主并记录环境 |
| 灰度/回滚 | 未执行 | 定义兼容窗口、指标、终止阈值和恢复目标 |

## 十二、明确未完成与 Rust 扩展
| 项目 | 分类 | 数量 | 覆盖处理 |
|---|---|---:|---|
| 无 Rust 对应 | `NOT_STARTED` | 13 | 不计实现/行为 |
| 占位实现 | `SKELETON` | 0 | 不计实现/行为 |
| 真实逻辑未差分 | `IMPLEMENTED_UNVERIFIED` | 6 | 计实现，不计行为 |
| 当前双基线行为验证 | `BEHAVIOR_VERIFIED` | 0 | 只有 E3/E4 或批准 oracle 才计入 |

## 十三、验证基线
| 证据级别 | 命令/环境 | 当前结果 | 结论 |
|---|---|---|---|
| E0 静态结构 | 固定 SHA 源码扫描 + layout audit | `1781 files; 217 errors; 23 warnings` | 结构仍不合格，不能证明语义 |
| E1 Rust 本地 | `cargo check/test -p hutool-bloom-filter` | 本轮文档任务未重跑 | 继承最近提交结果，不晋级对象状态 |
| E2 mirrored | 搜索 Rust 测试候选 | 存在候选但未完成逐源测试 disposition | 不等于差分 |
| E3 golden | Java 产物 + Rust comparator | 未建立当前双 SHA 元数据 | 0 个对象晋级 |
| E4 live | Java/Rust 同输入运行 | 未执行 | 0 个对象晋级 |
| E5-E8 | 并发/负载/fuzz/宿主/回滚 | 未执行 | 生产替代未证明 |

### 测试三本账
| Ledger | 完成/总数 | 当前缺口 |
|---|---:|---|
| SOURCE_PARITY | 0/3 个测试文件完成正式 disposition | 需要逐测试、参数化/动态用例登记 |
| RUST_OBLIGATION | 未统计 | 所有权、Drop、async、serde、feature、macro、adapter、unsafe |
| VALUE_ADD | 未统计 | property/fuzz/mutation/事故/负载/安全驱动 |
