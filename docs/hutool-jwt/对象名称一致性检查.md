# hutool-jwt 与 hutool-jwt 对象名称一致性检查

> 检查时间：2026-07-27
> 基线：Hutool 5.8.x `cn.hutool.jwt.*`（15 个 Java 类，1 根包 + 1 signers 子包）
> hutool-jwt crate：2 个 `.rs` 文件（`lib.rs` + `compat.rs`）

## 统计汇总

| 维度 | Hutool | hutool-jwt | 说明 |
|------|--------|------------|------|
| Java 类总数 | 15 | — | 排除 `package-info` |
| Rust 文件总数 | — | 2 | **严重违规**：1 个 `compat.rs` 1694 行 |
| **完全匹配** | — | **0** ⚠️ | 14 个类全部聚合在 `compat.rs` |
| Hutool 有 hutool-jwt 没有 | 15 | — | S1 拆分后全部补齐 |
| hutool-jwt 有 Hutool 没有 | — | 3 | Rust 特有（🆕） |
| 已合并（🔀） | — | 14 | 全部在 `compat.rs` |

> **关键问题**：hutool-jwt 是 hutool-rust **9 个 `compat.rs` 单源 dump 违规 crate 之一**。
> 当前 0 个完全匹配，因为 14 个 Java 类都聚合在 `compat.rs` 单文件中。S1 拆分完成后将
> 变为 15 个完全匹配 + 3 个 Rust 特有。

---

## 一、当前文件归属（拆分前）

### 1.1 `lib.rs`（128 行）

| 内容 | 类型 |
|------|------|
| `pub use jsonwebtoken::{Algorithm, Header, TokenData, Validation}` | re-export |
| `mod compat` | mod 声明 |
| `pub use compat::{...14 个类型}` | re-export 14 类 |
| `pub mod signers` | 子模块 |
| `JwtValidationPolicy` struct | 🆕 |
| `JwtHs256` struct + impl | 🆕 |
| `mod tests` | 单元测试 |

### 1.2 `compat.rs`（1694 行）🔴

| Java 类 | 行数估算 |
|---------|---------|
| `JWTException` | 50 |
| `JWTHeader` | 30 |
| `JWTPayload` | 100 |
| `RegisteredPayload` | 60 |
| `Claims` | 80 |
| `AlgorithmUtil` | 40 |
| `JWTSigner`（trait） | 30 |
| `JWTSignerUtil` | 60 |
| `NoneJWTSigner` | 30 |
| `HMacJWTSigner` | 60 |
| `AsymmetricJWTSigner` | 80 |
| `EllipticCurveJWTSigner` | 60 |
| `JWT` | 200 |
| `JWTUtil` | 100 |
| `JWTValidator` | 100 |
| 各种 helper 函数 | 604 |

---

## 二、S1 拆分后目标（15 个完全匹配）

### 2.1 根包子包（8 个 ✅）

| Hutool | hutool-jwt | 状态 |
|--------|------------|------|
| `JWT` | `jwt.rs` | ✅ |
| `JWTUtil` | `jwt_util.rs` | ✅ |
| `JWTValidator` | `jwt_validator.rs` | ✅ |
| `JWTException` | `jwt_exception.rs` | ✅ |
| `JWTHeader` | `jwt_header.rs` | ✅ |
| `JWTPayload` | `jwt_payload.rs` | ✅ |
| `RegisteredPayload` | `registered_payload.rs` | ✅ |
| `Claims` | `claims.rs` | ✅ |

### 2.2 signers 子包（7 个 ✅）

| Hutool | hutool-jwt | 状态 |
|--------|------------|------|
| `JWTSigner` | `signers/jwt_signer.rs` | ✅ |
| `JWTSignerUtil` | `signers/jwt_signer_util.rs` | ✅ |
| `AlgorithmUtil` | `signers/algorithm_util.rs` | ✅ |
| `NoneJWTSigner` | `signers/none_jwt_signer.rs` | ✅ |
| `HMacJWTSigner` | `signers/hmac_jwt_signer.rs` | ✅ |
| `AsymmetricJWTSigner` | `signers/asymmetric_jwt_signer.rs` | ✅ |
| `EllipticCurveJWTSigner` | `signers/elliptic_curve_jwt_signer.rs` | ✅ |

---

## 三、Rust 特有（🆕 3 个，无 Java 对偶）

| Rust 文件 | 语义 | 替代方案 |
|-----------|------|----------|
| `lib.rs::JwtValidationPolicy` | 显式 issuer/audience/leeway/exp 要求 | Java `JWTValidator` 隐式配置 |
| `lib.rs::JwtHs256` | HMAC-SHA256 codec（生产推荐） | Java `JWT.create().setSigner(HS256)` |
| `lib.rs::tests` | 完整编码 + 解码 + 拒绝用例 | Java 走 `JWTUtil.createToken` 等 |

---

## 四、命名风格检查

| 检查项 | Java | Rust | 结论 |
|--------|------|------|------|
| 类型命名 | `PascalCase` | `PascalCase` | ✅ 一致 |
| 方法命名 | `camelCase` | `snake_case` | ✅ 符合 Rust 惯例 |
| 字段命名 | `camelCase` | `snake_case` | ✅ 符合 Rust 惯例 |
| 常量命名 | `UPPER_SNAKE_CASE` | `UPPER_SNAKE_CASE` | ✅ 一致 |
| 文件名 | `PascalCase.java` | `snake_case.rs` | ✅ 符合 Rust 惯例 |
| 目录名 | 小写单词 | `snake_case` | ✅ 一致 |
| 异常后缀 | `*Exception` | `*Exception` | ✅ 一致（保留 `JWTException`） |
| 工具后缀 | `*Util` | `*Util` | ✅ 一致（`JWTUtil` / `JWTSignerUtil` / `AlgorithmUtil`） |
| 工厂后缀 | `*Factory` | — | ➖ 未出现 |
| 接口前缀 | 无（裸 `JWTSigner`） | 无（裸 `JWTSigner`） | ✅ 一致 |

> 注：hutool-jwt 是 hutool-rust 中**保留 Exception 后缀**的少数 crate 之一（而非 Rust
> 惯例的 Error），是因为对外 API 形态与 Hutool Java 端严格对齐，便于业务代码迁移。

---

## 五、待办（路线图后续阶段）

| 阶段 | 待补 | 优先级 |
|------|------|-------|
| S1 | 拆分 `compat.rs` 为 15 个文件 | 🔴 最高（违反红线） |
| S2 | JWT 核心 8 个类独立 | 🔴 高 |
| S3 | 签名抽象 3 个类独立 | 🔴 高 |
| S4 | 具体签名 4 个类独立 | 🔴 高 |
| S5 | JWTValidator 独立 | 🔴 高 |
| S6 | 完善每个新文件中文注释 | 🟡 中 |

---

## 六、与 AGENTS.md 红线对照

| 红线 | 当前状态 | 违规 |
|------|---------|------|
| ❌ 单文件不得超过 200 行 | `compat.rs` 1694 行 | ✅ 严重违规 |
| ❌ 不得用单一 `compat.rs` 集中定义所有对象 | `compat.rs` 集中 14 个类 | ✅ 严重违规 |
| ✅ 不允许 `unimplemented!()` / `todo!()` | 已确认无 | ✅ 合规 |
| ✅ 每个 .rs 文件对应一个 Java 对象 | 当前 1:14，**严重不满足** | ❌ 严重违规 |

> S1 拆分完成后，以上红线全部 ✅ 合规。

---

## 七、模块结构（拆分后目标）

```
hutool-jwt/
├── Cargo.toml
└── src/
    ├── lib.rs                       # crate 门面 + JwtValidationPolicy + JwtHs256 + tests
    ├── jwt.rs                       # JWT
    ├── jwt_util.rs                  # JWTUtil
    ├── jwt_validator.rs             # JWTValidator
    ├── jwt_exception.rs             # JWTException
    ├── jwt_header.rs                # JWTHeader
    ├── jwt_payload.rs               # JWTPayload
    ├── registered_payload.rs        # RegisteredPayload
    ├── claims.rs                    # Claims
    │
    ├── signers/
    │   ├── mod.rs                   # 子模块声明
    │   ├── jwt_signer.rs            # JWTSigner trait
    │   ├── jwt_signer_util.rs       # JWTSignerUtil
    │   ├── algorithm_util.rs        # AlgorithmUtil
    │   ├── none_jwt_signer.rs       # NoneJWTSigner
    │   ├── hmac_jwt_signer.rs       # HMacJWTSigner
    │   ├── asymmetric_jwt_signer.rs # AsymmetricJWTSigner
    │   └── elliptic_curve_jwt_signer.rs # EllipticCurveJWTSigner
    │
    └── tests/                       # 集成测试（按类拆分）
        ├── mod.rs
        ├── jwt_test.rs
        ├── jwt_util_test.rs
        ├── hmac_test.rs
        ├── rsa_test.rs
        └── ec_test.rs
```

---

## 八、状态汇总

| 维度 | 当前 | S1 拆分后目标 |
|------|------|--------------|
| Rust 文件数 | 2 | 18+ |
| 完全匹配 | 0 | 15 ✅ |
| 🔀 合并 | 14 | 0 |
| 🆕 Rust 特有 | 3 | 3 |
| 单文件最大行数 | 1694 | ≤ 200 |
| AGENTS.md 合规 | ❌ 严重违规 | ✅ |

---

> 文档维护：每次新增/拆分/合并 .rs 文件时同步更新本检查表
