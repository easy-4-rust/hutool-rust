# hutool-jwt 对象级对照表（验收清单）

> 检查时间：2026-07-27
> 基线：Hutool 5.8.x `cn.hutool.jwt.*`，15 个非 `package-info` Java 类
> hutool-jwt crate：2 个 `.rs` 文件（`lib.rs` + `compat.rs`）
> 规则：每个 Java 类对应一个 Rust `.rs` 文件（`PascalCase` ↔ `snake_case`）

> **重要警告**：当前 `src/compat.rs` 1694 行，**集中 14 个 Java 类**（`JWT` /
> `JWTUtil` / `Claims` / `JWTHeader` / `JWTPayload` / `RegisteredPayload` /
> `JWTException` / `JWTSigner` / `AlgorithmUtil` / `JWTSignerUtil` /
> `HMacJWTSigner` / `AsymmetricJWTSigner` / `EllipticCurveJWTSigner` /
> `NoneJWTSigner`），是 hutool-rust **9 个 `compat.rs` 单源 dump 违规 crate 之一**。
> 本表给出 S1 拆分目标。

本文档与 `语义迁移对照表.md` 互补：语义表回答"功能有没有"，本表回答"**每个 Java 对象
落在哪个 Rust 文件**"，是结构拆分与补缺的验收清单。

## 总览

| 维度 | Java | Rust |
|------|------|------|
| Java 子包数 | 1 + 1（signers） | 1 + 1 |
| Java 类总数（排除 package-info） | 15 | — |
| Rust 文件总数 | — | 2（**严重违规**） |
| 完全匹配 | — | 0 ⚠️ |
| 合并（🔀） | — | 14 |
| 待迁移（⬜） | — | 0 |
| 不迁移（🚫） | — | 0 |
| Rust 特有（🆕） | — | 3 |

---

## 命名与组织规则

1. 目录、文件名一律 `snake_case`；类型 `PascalCase`；方法 `snake_case`
2. **每个 .rs 文件只对应一个 Java 对象**；接口去 Java 匈牙利前缀
3. `mod.rs` 只做模块声明与 re-export，**禁止定义任何类型/逻辑**
4. Java 多层嵌套包只要求**最后一层目录**完全对齐
5. 每个文件头部必须有中文 doc 注释：说明对应 Java 类的全限定名、核心职责
6. 方法级中文注释从 Java 源码同步翻译

## 状态图例

| 标记 | 含义 |
|------|------|
| ✅ | 已迁移，独立文件已对齐 |
| 🔀 | 语义已迁移但与其他对象合并在同一文件，**待拆分** |
| ⬜ | 未迁移，缺失 |
| 🔶 | 语义等价但形态不同；文件保留，内部记录差异 |
| 🚫 | 不迁移（Java 生态特有），说明栏给出 Rust 化替代或理由 |
| 🆕 | Rust 侧新增、无 Java 对偶 |

---

## 一、`cn.hutool.jwt` 根包（7 Java → 7 Rust 🔀）

| Java 类 | 当前 Rust 文件 | 拆分目标 Rust 文件 | 状态 |
|---------|---------------|-------------------|------|
| `JWT` | `compat.rs` | `jwt.rs` | 🔀 |
| `JWTUtil` | `compat.rs` | `jwt_util.rs` | 🔀 |
| `JWTValidator` | `compat.rs` | `jwt_validator.rs` | 🔀 |
| `JWTException` | `compat.rs` | `jwt_exception.rs` | 🔀 |
| `JWTHeader` | `compat.rs` | `jwt_header.rs` | 🔀 |
| `JWTPayload` | `compat.rs` | `jwt_payload.rs` | 🔀 |
| `RegisteredPayload` | `compat.rs` | `registered_payload.rs` | 🔀 |
| `Claims` | `compat.rs` | `claims.rs` | 🔀 |

---

## 二、`cn.hutool.jwt.signers` 签名子包（7 Java → 7 Rust 🔀）

| Java 类 | 当前 Rust 文件 | 拆分目标 Rust 文件 | 状态 |
|---------|---------------|-------------------|------|
| `JWTSigner`（接口） | `compat.rs` | `jwt_signer.rs` | 🔀 |
| `JWTSignerUtil` | `compat.rs` | `jwt_signer_util.rs` | 🔀 |
| `AlgorithmUtil` | `compat.rs` | `algorithm_util.rs` | 🔀 |
| `NoneJWTSigner` | `compat.rs` | `none_jwt_signer.rs` | 🔀 |
| `HMacJWTSigner` | `compat.rs` | `hmac_jwt_signer.rs` | 🔀 |
| `AsymmetricJWTSigner` | `compat.rs` | `asymmetric_jwt_signer.rs` | 🔀 |
| `EllipticCurveJWTSigner` | `compat.rs` | `elliptic_curve_jwt_signer.rs` | 🔀 |

---

## 三、Rust 特有（🆕 3 个，无 Java 对偶）

| Rust 文件 | 语义 | 替代方案 |
|-----------|------|----------|
| `lib.rs::JwtValidationPolicy` | 显式 issuer/audience/leeway/exp 要求 | Java `JWTValidator` 隐式配置 |
| `lib.rs::JwtHs256` | HMAC-SHA256 codec（生产推荐） | Java `JWT.create().setSigner(HS256)` |
| `lib.rs::tests` | 完整编码 + 解码 + 拒绝用例 | Java 走 `JWTUtil.createToken` 等 |

---

## 四、待补清单（S1 拆分目标）

### 4.1 根包子包（8 个文件）

| Java 类 | 目标文件 | 估算行数 |
|---------|---------|---------|
| `JWT` | `jwt.rs` | 200 |
| `JWTUtil` | `jwt_util.rs` | 100 |
| `JWTValidator` | `jwt_validator.rs` | 100 |
| `JWTException` | `jwt_exception.rs` | 50 |
| `JWTHeader` | `jwt_header.rs` | 30 |
| `JWTPayload` | `jwt_payload.rs` | 100 |
| `RegisteredPayload` | `registered_payload.rs` | 60 |
| `Claims` | `claims.rs` | 80 |

### 4.2 signers 子包（7 个文件）

| Java 类 | 目标文件 | 估算行数 |
|---------|---------|---------|
| `JWTSigner` | `jwt_signer.rs` | 30 |
| `JWTSignerUtil` | `jwt_signer_util.rs` | 60 |
| `AlgorithmUtil` | `algorithm_util.rs` | 40 |
| `NoneJWTSigner` | `none_jwt_signer.rs` | 30 |
| `HMacJWTSigner` | `hmac_jwt_signer.rs` | 60 |
| `AsymmetricJWTSigner` | `asymmetric_jwt_signer.rs` | 80 |
| `EllipticCurveJWTSigner` | `elliptic_curve_jwt_signer.rs` | 60 |

> 拆分后 `compat.rs` 整体删除或仅保留 ≤ 30 行的 `mod` 声明。

---

## 五、模块结构（拆分后目标）

```
hutool-jwt/
├── Cargo.toml
└── src/
    ├── lib.rs                       # crate 门面 + JwtValidationPolicy + JwtHs256
    ├── jwt.rs                       # JWT
    ├── jwt_util.rs                  # JWTUtil
    ├── jwt_validator.rs             # JWTValidator
    ├── jwt_exception.rs             # JWTException
    ├── jwt_header.rs                # JWTHeader
    ├── jwt_payload.rs               # JWTPayload
    ├── registered_payload.rs        # RegisteredPayload
    ├── claims.rs                    # Claims
    │
    ├── signers/
    │   ├── mod.rs                   # 子模块声明
    │   ├── jwt_signer.rs            # JWTSigner trait
    │   ├── jwt_signer_util.rs       # JWTSignerUtil
    │   ├── algorithm_util.rs        # AlgorithmUtil
    │   ├── none_jwt_signer.rs       # NoneJWTSigner
    │   ├── hmac_jwt_signer.rs       # HMacJWTSigner
    │   ├── asymmetric_jwt_signer.rs  # AsymmetricJWTSigner
    │   └── elliptic_curve_jwt_signer.rs # EllipticCurveJWTSigner
    │
    └── tests/                       # 集成测试
        ├── mod.rs
        ├── jwt_test.rs
        ├── jwt_util_test.rs
        ├── hmac_test.rs
        ├── rsa_test.rs
        └── ec_test.rs
```

---

## 六、当前文件归属（拆分前）

### 6.1 `lib.rs`（128 行）

| 内容 | 类型 |
|------|------|
| `pub use jsonwebtoken::{Algorithm, Header, TokenData, Validation}` | re-export |
| `mod compat` | mod 声明 |
| `pub use compat::{...14 个类型}` | re-export 14 类 |
| `pub mod signers` | 子模块 |
| `JwtValidationPolicy` struct | 🆕 |
| `JwtHs256` struct + impl | 🆕 |
| `mod tests` | 单元测试 |

### 6.2 `compat.rs`（1694 行）🔴

| 内容 | 行数估算 |
|------|---------|
| `JWTException` enum | 50 |
| `JWTHeader` | 30 |
| `JWTPayload` | 100 |
| `RegisteredPayload` | 60 |
| `Claims` | 80 |
| `AlgorithmUtil` | 40 |
| `JWTSigner` trait | 30 |
| `JWTSignerUtil` | 60 |
| `NoneJWTSigner` | 30 |
| `HMacJWTSigner` | 60 |
| `AsymmetricJWTSigner` | 80 |
| `EllipticCurveJWTSigner` | 60 |
| `JWT` | 200 |
| `JWTUtil` | 100 |
| `JWTValidator` | 100 |
| 各种 helper 函数 | 604 |

---

## 七、状态汇总

| Java 子包 | Java 类 | Rust 现状 | S1 目标 |
|----------|--------|---------|---------|
| `cn.hutool.jwt` 根包 | 8 | 8 个聚合 | 8 个独立文件 |
| `cn.hutool.jwt.signers` | 7 | 7 个聚合 | 7 个独立文件 |
| **小计** | **15** | **15 个聚合** | **15+ 个独立文件** |
| Rust 特有 | — | 3 | 3 |

---

> 文档维护：每次新增/拆分/合并 .rs 文件时同步更新本对照表
