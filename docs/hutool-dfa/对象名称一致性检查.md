# hutool-dfa 与 cn.hutool.dfa 对象名称一致性检查

> 检查时间：2026-07-27
> 基线：Hutool 5.8.x hutool-dfa（cn.hutool.dfa.\*）
> hutool-dfa：10 个 Rust 文件 / 5 个 Java 类对应

## 统计汇总

| 维度 | Java | Rust | 说明 |
|------|------|------|------|
| 类/对象总数 | 5 | 5（一对一）| — |
| 文件总数（含 Rust 扩展） | 5 | 10 | 含 5 个 Rust 新增 + 3 个 mod.rs |
| **完全匹配** | **5** | **5** | PascalCase ↔ snake_case |
| Java 有 Rust 没有 | 0 | — | 全部已迁移 |
| Rust 有 Java 没有 | — | 5 | Rust 扩展 |

## ✅ 命名一致性 100% 达标

`hutool-dfa` 是 5 个目标模块中**唯一完全合规的 crate**：

- ✅ **5/5 Java 类全部独立成文件**
- ✅ **无 compat.rs dump 违规**
- ✅ **结构对齐 100%**
- 🆕 **5 个 Rust 扩展文件提供额外能力**

## 一、完全匹配的 5 个对象

### 顶层（2 个）
| Java 类 | Rust 目标文件 | 实际位置 | 状态 |
|---------|---------------|----------|------|
| `cn.hutool.dfa.SensitiveUtil` | `sensitive.rs` | `sensitive.rs` | ✅ |
| `cn.hutool.dfa.StopChar` | `stop_char.rs` | `stop_char.rs` | ✅ |

### word_tree（隐含类，2 个）
| Java 类 | Rust 目标文件 | 实际位置 | 状态 |
|---------|---------------|----------|------|
| `cn.hutool.dfa.WordTree` | `word_tree/word_tree.rs` | `word_tree/word_tree.rs` | ✅ |
| `cn.hutool.dfa.FoundWord` | `word_tree/found_word.rs` | `word_tree/found_word.rs` | ✅ |

### sensitive_processor（隐含接口，1 个）
| Java 类 | Rust 目标文件 | 实际位置 | 状态 |
|---------|---------------|----------|------|
| `cn.hutool.dfa.SensitiveProcessor` | `sensitive_processor/sensitive_processor.rs` | `sensitive_processor/sensitive_processor.rs` | ✅ |

## 二、Java 有但 Rust 没有的 0 个类

5/5 Java 类已全部命名映射到目标 Rust 文件 ✅

## 三、Rust 有但 Java 没有的 5 个文件

| Rust 文件 | 说明 |
|-----------|------|
| `lib.rs` | crate 门面 + `DfaError` 错误枚举 |
| `matcher/dfa_matcher.rs` | DfaMatcher trait（Rust 扩展） |
| `matcher/pattern_match.rs` | PatternMatch 结构（Rust 扩展） |
| `word_tree/match_options.rs` | MatchOptions 匹配配置（Rust 扩展） |
| `sensitive_processor/default_sensitive_processor.rs` | DefaultSensitiveProcessor 默认实现（Rust 扩展） |

## 四、命名规范验证

### 4.1 目录命名（snake_case）

- `word_tree/` ✅
- `sensitive_processor/` ✅
- `matcher/` ✅

### 4.2 文件命名（snake_case）

- `word_tree.rs` ✅
- `found_word.rs` ✅
- `match_options.rs` ✅
- `sensitive_processor.rs` ✅
- `default_sensitive_processor.rs` ✅
- `dfa_matcher.rs` ✅
- `pattern_match.rs` ✅
- `sensitive.rs` ✅
- `stop_char.rs` ✅

### 4.3 类型命名（PascalCase）

- `WordTree`, `FoundWord`, `MatchOptions` ✅
- `SensitiveProcessor`, `DefaultSensitiveProcessor` ✅
- `DfaMatcher`, `PatternMatch` ✅
- `SensitiveUtil`, `StopChar` ✅
- `DfaError` ✅

### 4.4 方法命名（snake_case）

- `WordTree::add_word` / `match_all` / `is_match` / `clear` ✅
- `SensitiveUtil::serialize` / `deserialize` / `get_found_all` / `get_found_first` ✅
- `SensitiveProcessor::process` ✅
- `DefaultSensitiveProcessor::new` ✅
- `StopChar::is_stop_char` / `is_not_stop_char` ✅
- `DfaMatcher::match_all` / `match_first` ✅
- `PatternMatch::new` / `with_metadata` ✅

## 五、语义一致性检查

### ✅ 完全一致的语义

- 字典树添加/查询
- 敏感词匹配
- 序列化/反序列化
- 停用字符判断
- 敏感词替换处理

### 🔶 形态不同

- `HashMap<Character, Map>` → `HashMap<char, WordTree>` 嵌套
- `ArrayList<FoundWord>` → `Vec<FoundWord>`
- `StringBuilder` → `String` + `push_str`

### 🆕 Rust 特有

- `MatchOptions`（重叠匹配配置）
- `DfaError`（错误枚举）
- `DfaMatcher`（独立 trait）
- `PatternMatch`（含元数据）
- `DefaultSensitiveProcessor`（默认实现）

## 六、文件头部注释规范验证

| 文件 | 对齐标注 | 来源标注 | 中文说明 | 状态 |
|------|----------|----------|----------|------|
| `lib.rs` | ✅ | ✅ | ✅ | ✅ |
| `sensitive.rs` | ✅ | ✅ | ✅ | ✅ |
| `stop_char.rs` | ✅ | ✅ | ✅ | ✅ |
| `word_tree/word_tree.rs` | ✅ | ✅ | ✅ | ✅ |
| `word_tree/found_word.rs` | ✅ | ✅ | ✅ | ✅ |
| `word_tree/match_options.rs` | ✅ | ✅ | ✅ | ✅ |
| `sensitive_processor/sensitive_processor.rs` | ✅ | ✅ | ✅ | ✅ |
| `sensitive_processor/default_sensitive_processor.rs` | ✅ | ✅ | ✅ | ✅ |
| `matcher/dfa_matcher.rs` | ✅ | ✅ | ✅ | ✅ |
| `matcher/pattern_match.rs` | ✅ | ✅ | ✅ | ✅ |

10/10 文件全部带完整中文注释头 ✅

## 七、总结

- ✅ **5/5 Java 类命名一致性 100%**
- ✅ **结构一致性 100%（无 dump）**
- 🆕 **5 个 Rust 扩展提供额外能力**
- 📝 **文件头部注释 100% 覆盖**
- 🟡 **测试覆盖待补**（`tests/dfa_parity.rs` 待建）

**结论**：hutool-dfa 是 5 个目标模块中**唯一完全合规**的 crate。可作为其他
crate（hutool-cache / hutool-captcha / hutool-cron / hutool-aop）参考的
结构模板。
